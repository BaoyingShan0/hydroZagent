## 这是一个非常典型的**“后台管理系统三页式”**设计需求。结合你提到的“智能体”和“审核规则库Skills”，设计的核心不在于“好看”，而在于**“降低审核员的认知负荷”**和**“将AI能力无缝嵌入操作流”**。

我为你设计了一套从**入口 → 总览 → 执行**的完整前端架构方案，并特别融入了AI辅助审核的交互细节：

---

### 一、 整体页面架构（三级路由设计）

建议采用 `一级入口` -> `二级列表` -> `三级详情` 的结构：

- **智能体主页 (Agent Home)**：放置一个显眼的悬浮按钮或Dashboard卡片。
- **审核任务总览 (Audit Dashboard)**：默认展示汇总表格，承载筛选与分发逻辑。
- **审核执行工作台 (Audit Workspace)**：核心审核界面，展示文件详情、规则命中情况及操作按钮。

---

### 二、 详细UI/UX设计方案

#### 1. 智能体主页的入口设计（触发点）
不要只放一个普通的文字链接，建议使用**“任务中心卡片”**：

- **视觉**：在主页右侧或中间区域放置 `待办审核` 卡片，右上角带红点气泡（显示待审核数量）。
- **交互**：点击卡片直接跳转 `/audit/list`。
- **附加逻辑**：如果当前待审核数量为0，卡片置灰并显示“暂无待办”，但仍可点击进入查看历史。

#### 2. 审核任务总览（汇总数据表格）设计
这是你的“战场指挥屏”，需要兼顾**筛选**与**快速介入**：

- **顶部统计看板（KPI Cards）**：用四个小卡片展示 `全部任务`、`待审核 (高危)`、`待审核 (常规)`、`已完成`。点击卡片可自动筛选表格。
- **表格列设计（关键）**：
  - **任务ID / 提交人 / 提交时间**（基础信息）
  - **文件摘要**：显示文件名+类型图标（PDF/Word/图片）。
  - **风险预判（AI亮点）**：调用智能体预先扫描，在此列显示“⚠️ 疑似违规x处”或“✅ 初步通过”。
  - **状态列（Status）**：使用高辨识度的标签（Tag）。
    - 🟡 **待审核**（黄色，带呼吸动画闪烁，提醒优先处理）
    - 🔵 **审核中**（蓝色，防止重复点击）
    - 🟢 **已审核**（绿色，对号图标）
    - ⚪ **未完成**（灰色，表示用户草稿未提交，审核员不可操作，置灰处理）
- **操作列**：放置 **“开始审核”** 按钮（仅当状态为“待审核”时可点），**“查看详情”**（已审核状态）。

> **交互细节**：点击表格行（除按钮外）可展开侧滑抽屉（Drawer），显示该任务的“AI预审摘要”，不离开列表页就能预览风险点，减少无效点击。

#### 3. 审核执行工作台（核心界面）设计
这是用户点击“开始审核”后进入的页面，建议采用 **“左-中-右”** 或 **“上-下”** 布局。鉴于审核文件，推荐**左右分栏**：

- **左侧（文件预览区，占比 60%）**：
  - 集成文档渲染组件（如PDF.js、图片查看器）。
  - 支持翻页、缩放。
  - **高亮联动**：右侧点击某条违规规则时，左侧自动滚动并高亮定位到文件具体位置。
- **右侧（审核操作区，占比 40%）**：
  - **顶部**：显示当前任务基本信息（编号、提交人）。
  - **中间（规则命中列表）**：展示智能体调用 `审核规则库Skills` 后的结果。
    - 每条规则显示：`规则编号`、`规则描述`、`命中原文片段`、`风险等级（高/中/低）`。
    - 每条规则旁有一个 **“采纳/忽略”** 开关，供人工复核修正。
  - **底部（结论区）**：
    - 大段的 **“审核意见”** 文本域（可支持AI一键生成审核评语，点击后自动填充）。
    - **核心按钮**：`审核通过`（绿色）、`驳回整改`（红色，需填驳回原因）、`转人工复核`（针对疑难杂症）。

---

### 三、 核心交互与状态流转（前端逻辑）

你需要在前端维护一个清晰的状态机（State Machine）：

1. **权限拦截**：`未完成` 状态的行，“开始审核”按钮置灰，并Tooltip提示“提交人尚未完成提交”。
2. **防止并发**：点击“开始审核”时，立即将该行状态在前端乐观更新为 `审核中`，并锁定该记录（防止团队内两人同时审同一份）。
3. **审核完成**：点击通过/驳回后，弹出二次确认Modal（防误触）。确认后调用API，成功后返回列表页，列表自动刷新，该行变更为 `已审核`。

---

### 四、 前端技术实现建议（落地层面）

| 模块 | 技术选型/实现建议 |
| :--- | :--- |
| **路由设计** | React Router (v6) 或 Vue Router，采用 **懒加载**。路径：`/audit` (列表) -> `/audit/:id` (详情) |
| **状态管理** | 使用 Zustand 或 Pinia。存储 `当前任务列表`、`筛选条件`。**注意**：审核详情页的数据（如规则命中列表）建议由当前页自己请求，不要全部存在Store里，避免数据过重。 |
| **表格组件** | 推荐 **Ant Design** 的 Table 或 **Element Plus** 的 Table。利用 `column slots` 自定义状态标签和操作按钮。 |
| **与智能体对接** | 在详情页 `useEffect` / `onMounted` 时，调用 **“获取AI预审结果”** 的接口（传入任务ID，返回命中的规则列表）。前端需设计 **“骨架屏”** 或 **“加载进度条”**，因为AI推理可能需要2-3秒，防止白屏焦虑。 |
| **规则库Skills联动** | 右侧规则列表不要写死。前端需解析后端返回的 `skill_id`，并动态渲染对应的“处理建议”（例如：涉及财务Skill，显示金额校验输入框；涉及法务Skill，显示条款引用）。 |

---

### 五、 针对“审核规则库Skills”的前端特殊设计（加分项）

既然你有规则库Skills，推荐在审核界面右侧增加一个 **“规则抽屉”** 功能：

- 审核员如果觉得AI漏判了，可以点击 **“手动补充规则”** 按钮。
- 弹窗中调用规则库Skills的查询输入框，输入关键词（如“版权”），系统自动列出库中相关规则，审核员勾选后，强制加入本次审核清单中。
- 这能让你的前端界面不仅仅是一个“查看器”，更是一个 **“人机协同的规则训练场”**。

---

### 六、 给你的开发排期建议（MVP优先级）

1. **Phase 1（基础版）**：先搭路由，做出列表页的静态表格（Mock数据），搞定状态标签的样式，实现点击“开始审核”跳转详情页。
2. **Phase 2（详情页）**：实现左右布局，文件预览用Iframe或开源库展示，右侧先放一个模拟的规则列表（硬编码）。
3. **Phase 3（AI接入）**：对接真实智能体API，替换模拟数据，加入Loading态。
4. **Phase 4（体验优化）**：加入高亮联动、一键生成审核意见、二次确认弹窗。

如果你告诉我你用的是 **React 还是 Vue**，以及 **UI 组件库是 Ant Design 还是 Element**，我可以给你具体的**代码片段（页面骨架和表格配置）**作为启动脚手架。需要吗？😊